version: "3.2"
services:
  jenkins:
    image: jenkins/jenkins:lts-alpine
    deploy:
      placement:
        constraints:
          - 'node.hostname == worker3'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 8080:8080
